# Stop Lombok from searching parent directories for further config.
config.stopBubbling = true

# Mark generated members with @lombok.Generated (helps coverage/static-analysis tooling).
lombok.addLombokGeneratedAnnotation = true

# Keep the project's existing logger field name instead of Lombok's default `log`.
lombok.log.fieldName = LOGGER
